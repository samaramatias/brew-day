'use strict';

(function () {
    var loginModule = angular.module('loginModule', []);

    loginModule.controller('LoginController', ['$rootScope',
        function ($rootScope) {

        }
    ]);
})();
